{% extends "base.html.twig" %}

{% block title %}
    {{servant.name}}
{% endblock %}
{% block body %}
    <div class="jumbotron">
        <div class="container">
            {# <h1>{{servant.name}}</h1> #}
            <div class="row">
                <div class="col-md-8">
                    {% if servant.filename %}
                        <img src="{{ vich_uploader_asset(servant, 'imagefile') | imagine_filter('medium') }}" alt="card-img-top" style="{# width: 100%; height: auto; #}">
                    {% endif %}
                </div>
                <div class="col-md-4">
                    <h1>{{servant.name}}</h1>
                    <h2>{{servant.classType}}</h2>
                    <div class="text-primary" style="font-size: 2rem; font-weight: bold;">{{servant.noblephantasme}}</div>
                    <a href="#" class="btn btn-primary">Invoquer le servant</a>
                </div>

            </div>


        </div>

    </div>   
    <div class="container mt-4">
    
    </div> 
{% endblock %}