{{ form_start(form) }}
<div class="row">
	<div class="col md-4">{{form_row(form.Name)}}</div>
	<div class="col md-4">{{form_row(form.Class)}}</div>
	<div class="col md-4">{{form_row(form.Noble_Phantasme)}}</div>
</div>
{# {{ form_rest(form) }} #}
<button class="btn btn-success">Enregistrer</button>
{{ form_end(form) }}
